language: java
jdk:
  - oraclejdk8
  #- oraclejdk7
  #- openjdk7
  - openjdk6
compiler:
  - gcc
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y subversion
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
script: ./RebuildNative
